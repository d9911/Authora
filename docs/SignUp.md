Нужно закрыть задачу по auth-flow и форме регистрации.

Проблема:
На http://localhost:5178/ я нажимаю на свой ник, после этого приложение переводит на авторизацию. Нужно проанализировать, почему так происходит, и исправить поведение.

Ожидаемое поведение:

1. Если пользователь уже авторизован, клик по нику НЕ должен переводить на страницу авторизации.
2. Если пользователь авторизован, клик по нику должен открывать профиль, меню пользователя или страницу аккаунта — в зависимости от текущей архитектуры проекта.
3. Если пользователь не авторизован, клик по защищённому user/profile разделу может переводить на авторизацию.
4. После успешного логина пользователь должен вернуться туда, куда он пытался попасть.
5. После логина не должно быть ошибки авторизации, пустого состояния или повторного редиректа на login.

Сначала сделай аудит:

- найди компонент, где отображается ник пользователя;
- найди обработчик клика по нику;
- найди auth store/context/session source;
- проверь, где хранится состояние авторизации;
- проверь, как определяется текущий пользователь;
- проверь protected routes / auth guard / redirects;
- проверь, сохраняется ли returnUrl/redirectTo после редиректа на login;
- проверь, не сбрасывается ли auth state после refresh/hydration.

Не исправляй наугад. Сначала покажи причину:

- какой файл делает redirect;
- почему пользователь считается неавторизованным;
- где теряется session/user/token;
- какой компонент вызывает переход на login.

Регистрация:
Нужно доработать форму регистрации.

Обязательные поля:

- password;
- confirmPassword.

Требования:

1. Пользователь вводит пароль два раза.
2. Если пароли не совпадают — показать понятную ошибку.
3. У обоих password-полей должна быть кнопка показать/скрыть пароль.
4. Кнопка показа пароля должна быть доступной: button, aria-label, focus-visible.
5. Ошибка должна появляться рядом с полем и не ломать layout.
6. Submit должен быть disabled или должен показывать ошибку, если пароль невалидный.

Password policy:
Если в проекте уже есть password policy — используй её.
Если нет — внедри такую политику:

Разрешённые символы:

- английские буквы: A-Z, a-z;
- испанская ñ в двух регистрах: Ñ, ñ;
- цифры: 0-9;
- 20 специальных символов:
  ! @ # $ % ^ & \* ( ) \_ + = . , ? : ~ [ ]

Запрещённые символы:

- одинарные кавычки;
- двойные кавычки;
- backtick;
- slash;
- backslash;
- angle brackets;
- semicolon;
- pipe;
- любые символы вне allowlist.

Лимит:

- минимум: если в проекте нет правила, поставь 8 символов;
- максимум: 50 символов.

Regex:
Создай одну общую константу для password regex, чтобы не дублировать её в разных местах.

Пример regex:
const PASSWORD*ALLOWED_REGEX = /^[A-Za-zÑñ0-9!@#$%^&\*()*+=.,?:~\[\]]{8,50}$/;

Важно:
Regex нужен только для UX-валидации и allowlist validation. Не считай regex защитой от SQL injection.

Security:
Проверь backend/API auth/register:

- пароль не должен сохраняться в plain text;
- пароль должен хешироваться современным password hashing algorithm;
- SQL-запросы должны быть parameterized/prepared statements или через безопасный ORM;
- нельзя собирать SQL строкой через конкатенацию password/email/name;
- client-side validation должна дублироваться server-side validation.

Текст подсказки:
Если пароль невалидный, покажи:
“Пароль должен быть 8–50 символов. Разрешены латинские буквы A–Z/a–z, Ñ/ñ, цифры 0–9 и символы: ! @ # $ % ^ & \* ( ) \_ + = . , ? : ~ [ ].”

Если пароли не совпадают:
“Пароли не совпадают.”

Проверки:
После исправления проверь:

- клик по нику авторизованного пользователя не ведёт на login;
- неавторизованный пользователь корректно попадает на login;
- после login пользователь возвращается на нужную страницу;
- ошибка авторизации не появляется после успешного входа;
- registration form требует confirmPassword;
- show/hide password работает для обоих полей;
- regex пропускает A-Z, a-z, Ñ, ñ, цифры и разрешённые спецсимволы;
- regex отклоняет запрещённые символы;
- пароль длиннее 50 символов отклоняется;
- ошибка показывает понятную подсказку;
- server-side validation тоже работает;
- нет TypeScript/lint/build ошибок.

Финальный ответ:

1. Укажи причину редиректа на авторизацию.
2. Укажи, какие файлы изменил.
3. Укажи, как теперь работает клик по нику.
4. Укажи, как теперь работает регистрация.
5. Укажи password regex и список разрешённых символов.
6. Укажи, как реализована защита от SQL injection.
7. Укажи, какие проверки запускал.
8. Если backend/password hashing/SQL слой не удалось проверить — скажи прямо.
